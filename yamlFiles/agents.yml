# agents.yml

source: LOCAL
target: MYSQL

streams:
  "C:/Users/cams9/SchoolWork/RESEARCH/moltbook-observatory-archive/data/agents/*.parquet":
    object: "moltbook_observatory.agents_tmp"
    mode: snapshot

    primary_key: id

    source_options:
      empty_as_null: true

    target_options:
      column_casing: snake
      column_types:
        id: varchar(255)
